name: sapiencial_app
description: App Educativa para Literatura Sapiencial.

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  
  # Gesti?n de Estado y Arquitectura
  provider: ^6.0.0
  
  # Conexi?n y Datos
  web_socket_channel: ^2.4.0
  http: ^1.1.0
  
  # UI y Estilos
  google_fonts: ^6.1.0
  cupertino_icons: ^1.0.2

  # PDF e impresion
  printing: ^5.11.0
  pdf: ^3.10.7

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true  
  assets:
    - assets/images/